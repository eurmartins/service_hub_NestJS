export enum StatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum OrderStatusEnum {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
