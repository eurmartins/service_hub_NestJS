export enum StatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum ServiceRequestStatusEnum {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
